#pragma once

#include <math.h>

static constexpr int   MAP_WIDTH  = 7000;
static constexpr int   MAP_HEIGHT = 3000;
static constexpr float MAP_MAX_DISTANCE = static_cast < float >(sqrt((MAP_WIDTH * MAP_WIDTH) + (MAP_HEIGHT * MAP_HEIGHT)));

