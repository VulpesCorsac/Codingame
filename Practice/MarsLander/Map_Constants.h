#pragma once

#include <math.h>

constexpr int   MAP_WIDTH  = 7000;
constexpr int   MAP_HEIGHT = 3000;
constexpr float MAP_MAX_DISTANCE = static_cast < float >(sqrt((MAP_WIDTH * MAP_WIDTH) + (MAP_HEIGHT * MAP_HEIGHT)));

